<%= form_with(model: beer_club) do |form| %>
  <% if beer_club.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
        <% beer_club.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, { class: "form-label"} %>
    <%= form.text_field :name, { class: "form-control", placeholder: "Club Name" } %>
  </div>

  <div>
    <%= form.label :founded, { class: "form-label"} %>
    <%= form.number_field :founded, { class: "form-control", placeholder: "Founding Year" } %>
  </div>

  <div>
    <%= form.label :city, { class: "form-label"} %>
    <%= form.text_field :city, { class: "form-control", placeholder: "City" } %>
  </div>

  <div>
    <button type="submit" class="btn btn-primary">Submit</button>
  </div>

<% end %>
